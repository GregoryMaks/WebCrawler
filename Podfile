# Uncomment this line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

target 'WebCrawler' do

    pod 'Alamofire', '~> 4.0'

	target 'WebCrawlerTests' do
		inherit! :search_paths
	end

end
